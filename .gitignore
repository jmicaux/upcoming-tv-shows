# Local secrets — real TMDB key lives here, never commit it
config.js

# Stray preferences export at the repo root — the kept copy lives in presets/
/lineup-preferences.json

# Local product/planning notes — not part of the shipped app
Lineup_Product_Roadmap.md

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/
