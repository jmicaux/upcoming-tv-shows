// Copy this file to config.js and fill in your key. config.js is git-ignored.
// A free TMDB API key (v3): https://www.themoviedb.org/settings/api
// Without it, the app still runs — only the Canal+ / Arte (French) data is skipped.
window.LINEUP_CONFIG = {
  TMDB_KEY: "YOUR_TMDB_API_KEY",
};
